Respond with a JSON array containing every action needed to solve the level. No further board state will be shown — plan the complete sequence now.
Add a "memory" field to the last action if useful.
Example:
  [{{example_action_1}}, {{example_action_2}}, {{example_action_2_with_memory}}]

Output the shortest sequence you are confident will solve the level.
